apiVersion: v1
kind: Service
metadata:
  name: student-app-backend-svc
  namespace: default
spec:
  selector:
    app: student-app-backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP